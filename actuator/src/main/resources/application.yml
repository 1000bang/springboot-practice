# ????? ??
management:
  server:
    port: 9292
  endpoints:
    web:
      exposure:
        include: "*"
  # ????? ???
  endpoint:
    shutdown:
      enabled: true
    health:
      #show-components: always
     show-details: always #detail? ?? ????? show-components
  info:
    java:
      enabled: true
    os:
      enabled: true
    env:
      enabled: true


info:
  app:
    name: hello-actuator
    company: bang
    version: 0.0.1

# loggin ?? ??
logging:
  level:
    hello.controller: debug
