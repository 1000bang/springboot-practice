my:
  datasource:
    url: local.db.com
    username: local_user
    password: local_pw
    etc:
      max-connection: 1
      timeout: 60s
      options: ADMIN, CACHE

test:
  jasypt:
    value: ENC(sUfK7ItVQ64isGvZc4hHFw==)

---
spring:
  config:
    activate:
      on-profile: dev
my:
  datasource:
    url: dev.db.com
    username: dev_user
    password: dev_pw
    etc:
      max-connection: 1
      timeout: 60s
      options: DEV, CACHE

---
spring:
  config:
    activate:
      on-profile: prd
my:
  datasource:
    url: prd.db.com
    username: prd_user
    password: prd_pw
    etc:
      max-connection: 50
      timeout: 10s
      options: PROD, CACHE
